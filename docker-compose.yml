version: '2'
services:
  webgoat:
    restart: always
    build: web
    container_name: webgoat
  ngrok:
    restart: always
    build: ngrok
    container_name: ngrok
    volumes:
      - ./.shared:/home/dev
    env_file: .env
    command: bash run.sh
    depends_on:
     - webgoat
